# Con el contenedor Docker del README (puerto 55432):
DATABASE_URL=postgresql+psycopg2://postgres:lubriexpress@localhost:55432/lubriexpress

# Con PostgreSQL instalado en la máquina:
# DATABASE_URL=postgresql+psycopg2://TU_USUARIO:TU_CONTRASEÑA@localhost:5432/lubriexpress

# Respaldo (scripts/respaldar.py)
# Carpeta de OneDrive donde dejar una copia; vacío = solo respaldo local.
# RESPALDO_ONEDRIVE=C:\Users\TU_USUARIO\OneDrive\Respaldos Lubri-Express
# Contenedor de Docker, solo si pg_dump no está instalado en el PC.
# RESPALDO_CONTENEDOR=lubriexpress-db
